# PReferZ 代码审查报告

> **审查日期**：2026-07-11
> **审查方式**：code-review skill（双轴并行：Standards + Spec）
> **审查范围**：全量源码（项目非 git 仓库，故以源码审查替代 diff 审查）
> **重点关注**：交互层（`interaction.rs` / `canvas.rs` / `viewport.rs` / `preferz_app.rs` / `transform_handles.rs`）
> **对照标准**：`AGENTS.md`（项目约定）+ `.agents/preferz-spec.md`（设计规格）+ Fowler 代码异味基线

---

## 一、Standards 轴

### 1.1 硬违规（违反 AGENTS.md 明文约定）

| # | 违规项 | 位置 | 违反约定 |
|---|--------|------|----------|
| S1 | 撤销栈绕过：`delete_selected` 直接 `scene.items.retain()` | [preferz_app.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/preferz_app.rs) `delete_selected` | AGENTS.md: "All item mutations go through the undo stack" |
| S2 | 撤销栈绕过：`process_import` 直接 `scene.add_item(item)` | `preferz_app.rs` `process_import` | 同上 |
| S3 | 撤销栈绕过：`bring_to_front`/`send_to_back` 直接写 `item.z = ...` 并自行 sort | `preferz_app.rs` | 同上；且绕过已有的 `Scene::bring_to_front` |
| S4 | 撤销栈绕过：`arrange.rs` 排列函数直接改 `item.transform.pos` | [arrange.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/arrange.rs) L27/58/80 | 同上 |
| S5 | `skip_first_redo` 形同虚设：`UndoStack::push` 从不调用 `redo()` 也不读该字段 | [commands.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/commands.rs) L9；[preferz_app.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/preferz_app.rs) L22 `UndoStack::push` | AGENTS.md Gotcha #5: "undo 预览模式 skip_first_redo" |
| S6 | 坐标系裸 f32：`Transform` 用 `Vector2D<f32, f32>`，unit 位填 `f32` 而非 `CanvasSpace` | [transform.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/transform.rs) L6-7 | AGENTS.md: "Use euclid types — never cast between them with raw f32" |
| S7 | 坐标系裸 f32：`Viewport.pan: (f32, f32)`、`screen_to_canvas -> (f32, f32)` | [viewport.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/viewport.rs) L5/36 | 同上 |
| S8 | 坐标系裸 f32：`canvas_dx = dx_screen / zoom` 裸换算；旋转手柄手算 atan2，未用 `Transform2D::with_anchor()` | `preferz_app.rs` L461/521-530 | AGENTS.md Gotcha #3 |
| S9 | 视口剔除缺失：`render_scene` 遍历全部 items 无 `screen_rect` 包含判断 | `preferz_app.rs` `render_scene` L541-636 | AGENTS.md: "viewport culling is mandatory" |
| S10 | 图像/rfd 阻塞主线程：`image::open()`、`rfd::FileDialog::pick_file()`、`save_file()` 均同步 | `preferz_app.rs` L642/652/692 | AGENTS.md: "Image decode runs on background threads"；Gotcha #4 |

### 1.2 交互正确性 Bug

| # | Bug | 位置 | 影响 |
|---|-----|------|------|
| B1 | `render_scene` 每帧 `TransformHandles::new()` 新建实例绘制，手柄绘制不反映 active/hover 状态 | `preferz_app.rs` L592/630 | 手柄视觉与实际状态脱节 |
| B2 | `bounding_rect` 自认不考虑旋转，hit-test 用 AABB | [item.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/item.rs) L109；[interaction.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/interaction.rs) L72-76；[transform_handles.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/ui/widgets/transform_handles.rs) L112 | 旋转后点击区域错误 |
| B3 | `content.len() as f32 * font_size/2.0` 用字节长度估宽 | `item.rs` L123 | CJK 文本宽度严重高估 |
| B4 | `DeleteItems::undo` 空实现 | `commands.rs` L157-159 | 删除后撤销无效，丢数据 |
| B5 | 每帧 `self.status = format!("缩放...")` 立即覆盖前面状态消息 | `preferz_app.rs` L353-354 | 状态栏信息丢失 |
| B6 | `update_hover` 在 hover（非按下）时就改写 `active_handle`/`drag_start_transform` | `transform_handles.rs` L56-66 | 拖拽状态机被拆散，逻辑难追踪 |

### 1.3 基线 Smell（判断题）

- **Duplicated Code**：
  - `render_scene` 的 Pixmap/Text 两臂（L590-594 / L628-632）手柄绘制完全相同
  - `arrange.rs::get_item_size`（L86）复刻 `item.rs::bounding_rect`（L113）的尺寸计算
  - `Scene::bring_to_front` 与 `PReferZApp::bring_to_front` 两套实现
- **Data Clumps**：`(screen_rect, pan, zoom)` 三元组在 `transform_handles.rs` L44/72/112 与 `preferz_app.rs` L151/223/378 反复同行，应捆成 `Viewport` 引用
- **Primitive Obsession**：`bounding_rect() -> (f32,f32,f32,f32)`（item.rs L109）、`drag_start_bounding: Option<(f32,f32,f32,f32)>`（transform_handles.rs L21）应建 `Rect2D<CanvasSpace>` 类型
- **Dead Code / Speculative Generality**：
  - `InteractionState`（interaction.rs，app 从未用，selection 用 `Scene.selection`）
  - `Selection` struct（selection.rs，未用）
  - `ContextMenu` widget（context_menu.rs 永返 `None`，真实菜单在 app 手写）
  - `TopBar`（全 TODO）、`Canvas`（canvas.rs 未接入）
  - `Scene::get_item_by_texture_id`（scene.rs L46）
  - `PReferZApp::undo/redo`（preferz_app.rs L700-706 仅 set status，真正逻辑在 update 内联）
- **Mysterious Name**：`transform.rs` L26 `new(pos, pos_y, ...)` 应为 `pos_x`；`arrange.rs` L101 `scene_next_z(item)` 实为 `item.z+1`，不查 scene
- **Feature Envy**：`interaction.rs` L67-69 `select_all` 直接 `scene.items.iter()`；`transform_handles.rs` L44 `update_hover` 接收 `&[Item]` 却只用 `bounding_rect()` 与 `id`

---

## 二、Spec 轴

### 2.1 缺失 / 未完成

#### Phase 3 交互（spec 标记未开始）

| # | 缺失项 | spec 行 | 现状 |
|---|--------|---------|------|
| M1 | 翻转边手柄 | L239「四角缩放、边界旋转区、翻转边」 | `Handle` 枚举无 Flip 变体；`flip_h`/`flip_v` 全项目无写入路径 |
| M2 | 框选 | L240 | `right_drag_start` 字段在但无框选矩形绘制/命中；右键只开菜单 |
| M3 | 多选统一外框 | L241 | `Selection::get_bounding_rect` 写了从未被调用；`render_scene` 对每项单独画手柄 |
| M4 | 文本便签 | L243 + L29「空文本自动删除」 | 无双击创建入口、无 `TextEdit` overlay；双击反被绑到 fit |
| M5 | 拖动移动 item | L28「自由移动」 | `MoveItems` 从未 push，选中后无法拖拽平移 |

#### Phase 5 撤销

| # | 缺失项 | spec 行 | 现状 |
|---|--------|---------|------|
| M6 | 未集成 undo crate | L261 | commands.rs 注释「不使用 undo crate」，自造 `Command` trait + `UndoStack` |
| M7 | skip_first_redo 死代码 | L291/L262 | 预览-推模式已实现但 skip 语义未生效 |
| M8 | 灰度/透明度/裁剪/排布 | L264-266 | 字段存在但无 UI；`arrange.rs` 从未被调用；无 `ArrangeItems` 命令 |

#### Phase 2

| # | 缺失项 | spec 行 | 现状 |
|---|--------|---------|------|
| M9 | 拖放导入 | L228（未勾选） | 无 `raw.dropped_files` 处理 |
| M10 | 后台线程解码 | §5.2 #3 | `process_import` 主线程同步 `image::open` |
| M11 | 视口裁剪 | §3.3 #1 | `render_scene` 无 culling |

### 2.2 范围蔓延

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| C1 | Debug 面板含「TEST: set drag」按钮 | `preferz_app.rs` L357-417 | spec 未要求 |
| C2 | `Ctrl+Shift+P` 开菜单、`Ctrl+Y` 重做 | `preferz_app.rs` L315-322/343 | spec 只规定 `Ctrl+Shift+Z` |
| C3 | 提前 stub 的 Phase 5/6 模块 | `welcome.rs`/`export.rs`/`arrange.rs`/`context_menu.rs` | 未接入却占坑 |

### 2.3 实现但有误

| # | 项 | spec 行 | 问题 |
|---|----|---------|------|
| W1 | Transform 丢空间类型 | §3.4/§5.1 #3 | 要求 `Vector2D<f32, CanvasSpace>`，实际 unit 是 `f32`；全项目无 `with_anchor` |
| W2 | hit_test 不用逆变换 | §7.2 | 要求 transform 逆变换，实际仅 AABB，旋转后命中错误 |
| W3 | 视口两函数中心不一致 | §7.1 | `screen_to_canvas` 用 `screen_size/2`，`canvas_to_screen` 用 `screen_rect.center()`，非互逆；`fit_to_content` 与 `fit_to_screen` 对 pan 定义不同 |
| W4 | zoom 鼠标居中公式量纲错 | §7.1 | 屏幕像素 `center.x` 与画布空间 `pan.0` 混算 |
| W5 | 缩放手柄数学错 | L242 | 强制 `scale.x=scale.y` 后只平移单轴，对角未真正固定；`start_cw*(1.0-base_scale)` 量纲不对；未用 `with_anchor` |
| W6 | 旋转中心漂移 | L242 | 每帧用 AABB 重算中心，旋转时中心会飘 |
| W7 | `DeleteItems::undo` 空实现 | §3.5 | 丢数据 |
| W8 | 删除/置顶绕过 undo 与 scene | §5.2 #1 | 违反第一约定 |
| W9 | `next_z` 不维护 + 渲染忽略 Z 序 | §3.4 | `add_item` 不赋 z/不自增，item 全 z=0 同级；`render_scene` 按 `items` 原序而非 `items_by_z_order()` 渲染 |
| W10 | Text 宽度按字节 | — | `content.len()`（字节）×font_size/2，CJK 宽度错误 |

> Phase 4 的 `bee.rs`（`load_scene` 返回空场景、`save_scene` 无增量/VACUUM）属未开始阶段，暂缓。

---

## 三、一行总结

| 轴 | 发现数 | 本轴最严重 |
|---|--------|-----------|
| **Standards** | 5 硬违规 + 6 交互 bug + 6 类 smell | **撤销栈被多路径绕过 + `skip_first_redo` 形同虚设**（破坏 AGENTS.md 第一条铁律） |
| **Spec** | 11 项缺失 + 3 项蔓延 + 10 项实现有误 | **Phase 3 交互几乎全未实现，且已写的缩放/旋转/hit-test 数学有错**（zoom 量纲错、旋转中心漂移、未用 `with_anchor`） |

---

## 四、后续开发建议（按优先级排序）

> 原则：**先堵漏、再补缺、后优化**。P0/P1 不解决，做任何 Phase 3 新交互都会持续累积 bug。

### P0 — 立即修复的交互正确性 Bug（地基）

- [ ] **P0-1 修坐标系地基**：`Transform` 改回 `Vector2D<f32, CanvasSpace>`；`Viewport` 用 `Vector2D<f32, ScreenSpace>`；`pan`/`screen_to_canvas` 全部走 euclid 类型。这一步会顺带暴露所有裸 f32 换算点。（对应 S6/S7/S8/W1）
- [ ] **P0-2 统一 viewport 坐标公式**：让 `screen_to_canvas` 与 `canvas_to_screen` 严格互逆（都用 `screen_rect.center()`）；修 zoom 鼠标居中的量纲错误。（对应 W3/W4）
- [ ] **P0-3 修 hit-test**：用 `item.transform.inverse().transform_point(canvas_pos)` 落到局部坐标再判 OBB，旋转后才正确（spec §7.2 原话）。（对应 B2/W2）
- [ ] **P0-4 修缩放/旋转数学**：换 `euclid::Transform2D::with_anchor(角点)` 做缩放，旋转中心用变换前后的同一锚点（不是每帧重算 AABB 中心）。（对应 W5/W6/S8）
- [ ] **P0-5 修 `render_scene` 的 TransformHandles**：不要每帧 `new()`，复用 `self.transform_handles` 状态绘制，否则手柄 hover/active 视觉与实际不符。（对应 B1）

### P1 — 补齐 undo 契约（AGENTS.md 第一铁律）

- [ ] **P1-1 让 `skip_first_redo` 真正生效**：`UndoStack::push` 接收一个 `preview_already_applied: bool`，若 true 则跳过首次 `redo()`。所有交互预览（拖拽/缩放/旋转）释放时传 true。（对应 S5/M7）
- [ ] **P1-2 所有 item 变更走命令**：`delete_selected` 用 `DeleteItems`、`process_import` 用 `AddItem`、置顶/置底用 `Scene::bring_to_front` 并包成 `ReorderItems` 命令、`arrange.rs` 返回 delta 由 app push `MoveItems`。（对应 S1/S2/S3/S4/W8）
- [ ] **P1-3 补 `DeleteItems::undo`**：当前空实现会丢数据，必须存被删 item 快照。（对应 B4/W7）
- [ ] **P1-4 维护 `next_z` + 渲染按 Z 序**：`add_item` 赋 `z = next_z; next_z += 1`；`render_scene` 改用 `scene.items_by_z_order()`。（对应 W9）

### P2 — 补 Phase 3 缺失交互（spec L237-246）

- [ ] **P2-1 拖动移动**：选中按下拖拽 → 预览改 `pos`，释放 push `MoveItems`（`skip_first_redo=true`）。这是交互的"hello world"，先做。（对应 M5）
- [ ] **P2-2 框选**：右键/Shift+左键拖拽成矩形，画虚线框，释放时 `scene.selection = hit_test(rect)`。（对应 M2）
- [ ] **P2-3 多选统一外框**：`Selection::get_bounding_rect` 已经写好了，接进 `render_scene` 对多选画一个统一 bbox + 手柄。（对应 M3）
- [ ] **P2-4 翻转手柄**：`Handle` 加 `FlipH`/`FlipV` 边中点变体，触发 `flip_h = !flip_h`（包 `FlipItems` 命令）。（对应 M1）
- [ ] **P2-5 文本便签**：双击空白处创建 `TextItem{editing:true}`，在 item 屏幕位置 overlay `TextEdit`，失焦或 Enter 退出编辑，空内容自动删除。（对应 M4）
- [ ] **P2-6 Delete 键**：已有绑定，确保走 `DeleteItems` 命令。（与 P1-2 协同）

### P3 — 补 Phase 2 未勾选项 + 性能

- [ ] **P3-1 拖放导入**：`update()` 读 `ctx.input(|i| i.raw.dropped_files.clone())`，路径走与菜单导入同一入口。（对应 M9）
- [ ] **P3-2 后台解码**：`process_import` 改 `std::thread::spawn` + channel + `egui_ctx.request_repaint()`；rfd 大目录包线程。（对应 M10/S10）
- [ ] **P3-3 视口剔除**：`render_scene` 加 `if !screen_rect.intersects(item_screen_rect) { continue; }`，spec §3.3 明文 mandatory。（对应 S9/M11）

### P4 — 清理（避免后续踩坑）

- [ ] **P4-1 删死代码**：`InteractionState`、`Selection` struct、`ContextMenu` stub、未接入的 `Canvas`/`TopBar`，要么接入要么删除，别留半成品占坑误导。
- [ ] **P4-2 抽 `Viewport` 引用**消除 `(screen_rect, pan, zoom)` data clump；`bounding_rect` 返回 `Rect2D<CanvasSpace>` 消除 primitive obsession。
- [ ] **P4-3 修小问题**：Text 宽度按字节（B3）；状态消息覆盖（B5）；`update_hover` 状态机拆散（B6）；命名修正（`pos_y`→`pos_x`、`scene_next_z`）。

### 建议节奏

- **下一阶段**：聚焦 **P0 + P1**（共 9 项），不解决这两个地基问题，做任何 Phase 3 新交互都会持续累积 bug。
- **再下一阶段**：P2-1（拖动移动）作为 Phase 3 第一个落地功能，验证 P0/P1 修复是否到位，再铺开 P2 其余项。
- **并行可做**：P3-3（视口剔除）与 P4-1（删死代码）可随时穿插。

---

## 五、参考索引

- 项目约定：[AGENTS.md](file:///e:/play/coding/PReferZ/AGENTS.md)
- 设计规格：[.agents/preferz-spec.md](file:///e:/play/coding/PReferZ/.agents/preferz-spec.md)
- 关键交互文件：
  - [interaction.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/interaction.rs)
  - [canvas.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/ui/canvas.rs)
  - [viewport.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/viewport.rs)
  - [preferz_app.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/preferz_app.rs)
  - [transform_handles.rs](file:///e:/play/coding/PReferZ/crates/preferz/src/ui/widgets/transform_handles.rs)
- 关键领域文件：
  - [transform.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/transform.rs)
  - [item.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/item.rs)
  - [commands.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/commands.rs)
  - [scene.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/scene.rs)
  - [selection.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/selection.rs)
  - [arrange.rs](file:///e:/play/coding/PReferZ/crates/preferz-core/src/arrange.rs)
